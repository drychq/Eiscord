{
  "name": "eiscord",
  "version": "0.1.0",
  "private": true,
  "description": "Discord-like realtime community collaboration system.",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @eiscord/shared build && pnpm --filter @eiscord/media build && pnpm --parallel --filter @eiscord/api --filter @eiscord/web dev",
    "dev:api": "pnpm --filter @eiscord/shared build && pnpm --filter @eiscord/media build && pnpm --filter @eiscord/api dev",
    "dev:web": "pnpm --filter @eiscord/shared build && pnpm --filter @eiscord/web dev",
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm --filter @eiscord/shared build && pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "test:e2e:api": "pnpm --filter @eiscord/shared build && pnpm db:test:reset && DATABASE_URL=postgresql://eiscord:eiscord@localhost:5432/eiscord_test REDIS_CONNECT_IN_TEST=true REALTIME_SWEEP_IN_TEST=true PRESENCE_SWEEP_INTERVAL_MS=100 PRESENCE_OFFLINE_GRACE_MS=120 pnpm --filter @eiscord/api test:e2e",
    "test:e2e:web": "pnpm --filter @eiscord/shared build && pnpm --filter @eiscord/media build && pnpm prepare:e2e:audio && pnpm db:test:reset && DATABASE_URL=postgresql://eiscord:eiscord@localhost:5432/eiscord_test pnpm db:seed && pnpm exec playwright test",
    "test:e2e:voice": "pnpm --filter @eiscord/shared build && pnpm --filter @eiscord/media build && pnpm prepare:e2e:audio && pnpm db:test:reset && DATABASE_URL=postgresql://eiscord:eiscord@localhost:5432/eiscord_test pnpm db:seed && pnpm exec playwright test tests/e2e/voice-media.spec.ts",
    "test:e2e": "pnpm test:e2e:api && pnpm test:e2e:web",
    "perf:k6": "k6 run scripts/k6/m6-realtime-load.js",
    "prepare:e2e:audio": "node scripts/generate-test-audio.mjs",
    "lint": "pnpm -r --if-present lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,cjs,mjs,json,md,css,yml,yaml}\"",
    "db:generate": "prisma generate --schema=prisma/schema.prisma",
    "db:migrate": "prisma migrate dev --schema=prisma/schema.prisma",
    "db:test:reset": "docker compose up -d postgres redis minio minio-init && docker compose exec -T postgres sh -lc 'dropdb -U eiscord --if-exists eiscord_test && createdb -U eiscord eiscord_test' && DATABASE_URL=postgresql://eiscord:eiscord@localhost:5432/eiscord_test pnpm db:generate && DATABASE_URL=postgresql://eiscord:eiscord@localhost:5432/eiscord_test pnpm exec prisma migrate deploy --schema=prisma/schema.prisma",
    "db:seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.13.0"
  },
  "devDependencies": {
    "@eiscord/config": "workspace:*",
    "@eslint/js": "^9.0.0",
    "@playwright/test": "^1.43.1",
    "eslint": "^9.0.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.4.5",
    "globals": "^15.0.0",
    "prettier": "^3.2.5",
    "prisma": "^5.13.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.5",
    "typescript-eslint": "^8.0.0",
    "vitest": "^1.6.0"
  }
}
